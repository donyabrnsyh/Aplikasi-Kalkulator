function insert(parameter) {
  call.layar.value += parameter;
}

function ac() {
  call.layar.value = "";
}

function equal() {
  call.layar.value = eval(call.layar.value);

  // if (eval(call.layar.value) === parseFloat) {
  //   return parsefloat(call.layar.valeu);
  //   }
}
